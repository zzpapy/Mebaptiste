{% extends 'base.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block body %}
    <h1>{{ page.title }}</h1>

    {% if page.featuredImage %}
        <img src="{{ asset('uploads/pages/' ~ page.featuredImage) }}" alt="{{ page.title }}" style="max-width: 300px; border-radius: 8px;">
    {% endif %}

    <div>{{ page.content|raw }}</div>
{% endblock %}